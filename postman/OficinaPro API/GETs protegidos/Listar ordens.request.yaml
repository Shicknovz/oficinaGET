$kind: http-request
url: "{{baseUrl}}/ordens"
method: GET
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: pm.test('Ordens listadas', function () { pm.response.to.have.status(200); });
order: 2000
