$kind: http-request
url: "{{baseUrl}}/agendamentos"
method: GET
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: pm.test('Agendamentos listados', function () { pm.response.to.have.status(200); });
order: 3000
