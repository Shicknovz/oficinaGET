$kind: http-request
url: "{{baseUrl}}/pecas"
method: GET
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: pm.test('Peças listadas', function () { pm.response.to.have.status(200); });
order: 4000
